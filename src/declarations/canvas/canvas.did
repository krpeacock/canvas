type Result = 
 variant {
   err: Error;
   ok: record {
         nat;
         nat;
         nat;
       };
 };
type Image = vec vec text;
type Error = 
 variant {
   BadRequest;
   InsufficientCycles;
 };
service : {
  getHistory: () -> (vec Image) query;
  latest: () -> (Image) query;
  upload: (Image) -> (Result);
}
